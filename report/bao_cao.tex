\documentclass[12pt,oneside]{report}

% ---------- Gói lệnh ----------
\usepackage[T5,T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[utf8]{vntex}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc,shapes.geometric,fit,backgrounds}
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=black, citecolor=black, urlcolor=blue}

\renewcommand{\arraystretch}{1.15}

\tikzset{
  gatebox/.style={draw, rounded corners=2pt, minimum width=0.8cm, minimum height=0.7cm, fill=orange!25, font=\small},
  tanhbox/.style={draw, rounded corners=2pt, minimum width=0.8cm, minimum height=0.7cm, fill=blue!15, font=\small},
  opnode/.style={draw, circle, minimum size=0.42cm, inner sep=0pt, font=\footnotesize},
  dotn/.style={draw, circle, fill=black, minimum size=2.5pt, inner sep=0pt},
  cline/.style={-{Latex[length=2mm]}, thick},
  realcell/.style={draw, minimum width=0.8cm, minimum height=0.7cm, fill=blue!20, font=\scriptsize},
  predcell/.style={draw, minimum width=0.8cm, minimum height=0.7cm, fill=orange!35, font=\scriptsize},
  grubox/.style={draw, rounded corners=2pt, minimum width=1.1cm, minimum height=0.7cm, fill=green!15, font=\small},
}

\begin{document}

% ==================== TRANG BÌA ====================
\begin{titlepage}
\begin{center}

\includegraphics[width=3.2cm]{figures/logo.png}\\[0.4cm]
{\large\bfseries TRƯỜNG ĐẠI HỌC QUY NHƠN}\\[1.2cm]

{\Large\bfseries TIỂU LUẬN HỌC PHẦN}\\[0.3cm]
{\Large\bfseries HỌC SÂU VÀ ỨNG DỤNG}\\[0.5cm]
\rule{0.85\textwidth}{0.5pt}\\[0.8cm]

{\LARGE\bfseries ỨNG DỤNG MẠNG GRU DỰ BÁO NỒNG ĐỘ PM2.5\\[0.2cm] TẠI THÀNH PHỐ QUY NHƠN}\\[0.5cm]
\rule{0.85\textwidth}{0.5pt}\\[2cm]

\begin{flushleft}
\hspace{3.5cm}
\begin{tabular}{ll}
Trình độ đào tạo: & Thạc sĩ \\[2pt]
Học viên thực hiện: & Nguyễn Hồ Bảo Thiên \\[2pt]
Lớp: & Khoa học dữ liệu K28B \\[2pt]
Giảng viên hướng dẫn: & TS. Lê Xuân Vinh \\[2pt]
\end{tabular}
\end{flushleft}

\vfill
Gia Lai, 2026

\end{center}
\end{titlepage}

% ==================== MỤC LỤC ====================
\tableofcontents
\newpage

% \listoffigures và \listoftables trong report-class đều tự khởi đầu bằng \chapter*,
% mà \chapter luôn ngắt sang trang mới — nên nếu gọi liên tiếp, "Danh sách bảng" sẽ
% bị đẩy sang trang riêng dù còn dư chỗ. Ở đây chỉ để \listoffigures ngắt trang một
% lần, còn danh sách bảng được chèn thủ công (không ngắt trang) để cả hai nằm chung 1 trang.
\listoffigures
\bigskip
\makeatletter
{\Large\bfseries\listtablename\par}
\@mkboth{\MakeUppercase\listtablename}{\MakeUppercase\listtablename}
\@starttoc{lot}
\makeatother
\newpage

% ==================== MỞ ĐẦU ====================
\chapter*{Mở đầu}
\addcontentsline{toc}{chapter}{Mở đầu}

Ô nhiễm không khí đang trở thành một trong những vấn đề môi trường đáng quan tâm tại các khu vực đô thị, trong đó bụi mịn PM2.5 là một trong những tác nhân gây ảnh hưởng nghiêm trọng đến sức khỏe con người. PM2.5 là các hạt vật chất có đường kính khí động học không vượt quá $2.5,\mu m$. Với kích thước rất nhỏ, các hạt này có khả năng xâm nhập sâu vào hệ hô hấp, thậm chí đi vào hệ tuần hoàn, từ đó làm gia tăng nguy cơ mắc các bệnh liên quan đến đường hô hấp và tim mạch. Vì vậy, việc dự báo sớm nồng độ PM2.5, đặc biệt trong khoảng thời gian ngắn, có ý nghĩa quan trọng đối với công tác cảnh báo chất lượng không khí, hỗ trợ cộng đồng chủ động bảo vệ sức khỏe và góp phần nâng cao hiệu quả quản lý môi trường đô thị.

Trong tiểu luận này, bài toán được xây dựng dưới dạng \textbf{dự báo chuỗi thời gian (time series forecasting)}, với mục tiêu dự báo nồng độ PM2.5 trong một khoảng thời gian tới, dựa trên lịch sử quan trắc trong một khoảng thời gian trước đó. Dữ liệu đầu vào được tổ chức dưới dạng chuỗi thời gian đa biến, bao gồm nồng độ PM2.5, các chỉ số chất lượng không khí có liên quan và các đặc trưng về thời gian. Mô hình học sâu được lựa chọn là \textbf{GRU (Gated Recurrent Unit)}, một biến thể của mạng nơ-ron hồi quy (Recurrent Neural Network -- RNN) được thiết kế nhằm học các mối quan hệ phụ thuộc theo thời gian và khắc phục một số hạn chế của RNN truyền thống khi xử lý các chuỗi có phụ thuộc dài hạn.

Bộ dữ liệu sử dụng trong tiểu luận được thu thập từ \textbf{Open-Meteo Air Quality API} tại tọa độ $13.800003^\circ$N, $109.20001^\circ$E, tương ứng với khu vực thành phố Quy Nhơn, ở độ cao khoảng \textbf{9 m so với mực nước biển}. Dữ liệu được ghi nhận theo tần suất hàng giờ, liên tục từ \texttt{2022-08-04 07:00} đến \texttt{2026-07-26 23:00}, với tổng cộng \textbf{34.865 quan sát}. Khoảng thời gian quan trắc kéo dài gần bốn năm cung cấp cơ sở dữ liệu đủ lớn để khảo sát các đặc trưng theo thời gian của PM2.5 cũng như huấn luyện và đánh giá mô hình dự báo.

Trên cơ sở bài toán và mục tiêu nghiên cứu đã đặt ra, tiểu luận được triển khai theo một quy trình gồm các nội dung chính sau:

\begin{itemize}
\item Thu thập, kiểm tra và làm sạch dữ liệu;
\item Phân tích khám phá dữ liệu (EDA) theo đặc trưng của chuỗi thời gian, tập trung vào xu hướng, tính chu kỳ và cấu trúc tự tương quan;
\item Tiền xử lý dữ liệu và xây dựng đặc trưng, bao gồm mã hóa các đặc trưng thời gian có tính chu kỳ, chuẩn hóa dữ liệu và xây dựng các cửa sổ quan sát bằng phương pháp sliding window;
\item Xây dựng mô hình GRU và tiến hành tinh chỉnh các siêu tham số phù hợp;
\item Đánh giá khả năng dự báo của mô hình trên tập kiểm tra, đồng thời kiểm chứng trên một tập dữ liệu hoàn toàn mới được thu thập ở giai đoạn sau nhằm đánh giá khả năng tổng quát hóa trong điều kiện \textit{out-of-sample} thực tế.
\end{itemize}

Trên cơ sở đó, báo cáo sẽ trình bày lần lượt quá trình chuẩn bị dữ liệu, phân tích đặc trưng chuỗi thời gian, xây dựng và huấn luyện mô hình, đánh giá kết quả dự báo, đồng thời thảo luận những kết quả đạt được và các hạn chế còn tồn tại của mô hình.

% ==================== CHƯƠNG 1: CƠ SỞ LÝ THUYẾT ====================
\chapter{Cơ sở lý thuyết}

\section{Bài toán dự báo chuỗi thời gian bằng học sâu}

Khác với các bài toán học có giám sát thông thường, dữ liệu chuỗi thời gian có tính tuần tự: giá trị tại thời điểm $t$ có quan hệ phụ thuộc với các giá trị trong quá khứ. Cách tiếp cận phổ biến để chuyển bài toán dự báo chuỗi thời gian thành bài toán học có giám sát là kỹ thuật \textbf{sliding window}: với một cửa sổ quan sát độ dài $w$ (window size) và một khoảng dự báo $h$ (horizon), ta xây dựng cặp dữ liệu

\[
X_t = (x_{t-w+1}, x_{t-w+2}, \dots, x_t) \quad \longrightarrow \quad y_t
\]

trong đó $x_i \in \mathbb{R}^n$ là vector $n$ đặc trưng tại thời điểm $i$, còn $y_t$ là giá trị của biến mục tiêu (biến cần dự báo) tại bước thời gian $t+h$ trong tương lai.

Phần tiếp theo trình bày cơ sở lý thuyết của các kiến trúc mạng nơ-ron hồi quy theo trình tự \textbf{RNN $\rightarrow$ LSTM $\rightarrow$ GRU}. Trước hết, RNN truyền thống được giới thiệu cùng với cơ chế xử lý dữ liệu tuần tự và những hạn chế trong việc học các phụ thuộc dài hạn. Tiếp đó, LSTM được trình bày như một kiến trúc cải tiến nhằm khắc phục các hạn chế này thông qua cơ chế bộ nhớ và các cổng điều khiển. Cuối cùng, GRU --- kiến trúc được sử dụng trong tiểu luận --- được giới thiệu như một biến thể đơn giản hơn của LSTM, với số lượng cổng và tham số ít hơn nhưng vẫn có khả năng mô hình hóa các mối quan hệ phụ thuộc theo thời gian.

\section{Recurrent Neural Network (RNN)}

Recurrent Neural Network (RNN) là một kiến trúc mạng nơ-ron được xây dựng để xử lý dữ liệu có tính tuần tự, chẳng hạn như chuỗi thời gian, văn bản hoặc tín hiệu. Khác với mạng nơ-ron truyền thẳng (\textit{feedforward neural network}), trong đó mỗi đầu vào được xử lý độc lập, RNN duy trì một \textbf{trạng thái ẩn} (\textit{hidden state}) để truyền thông tin từ các bước thời gian trước sang bước hiện tại. Nhờ đó, kết quả xử lý tại một thời điểm không chỉ phụ thuộc vào quan sát hiện tại mà còn chịu ảnh hưởng bởi thông tin đã xuất hiện trước đó trong chuỗi.

Tại thời điểm $t$, RNN nhận đầu vào $x_t$ cùng với trạng thái ẩn của bước trước $h_{t-1}$ để tính trạng thái ẩn mới $h_t$ theo công thức:

\begin{equation}
h_t = \tanh(W_h x_t + U_h h_{t-1} + b_h),
\end{equation}

trong đó $x_t \in \mathbb{R}^{n}$ là vector đặc trưng tại thời điểm $t$; $h_{t-1}$ và $h_t$ lần lượt là trạng thái ẩn tại thời điểm $t-1$ và $t$; $W_h$ là ma trận trọng số liên kết đầu vào với trạng thái ẩn; $U_h$ là ma trận trọng số của kết nối hồi quy giữa hai trạng thái ẩn liên tiếp; $b_h$ là vector bias; và $\tanh(\cdot)$ là hàm kích hoạt phi tuyến. Trạng thái ban đầu $h_0$ thường được khởi tạo bằng vector không hoặc được xác định theo cấu hình của mô hình.

Thông qua quan hệ hồi quy trên, trạng thái $h_t$ phụ thuộc không chỉ vào $x_t$ mà còn gián tiếp phụ thuộc vào các quan sát trước đó thông qua $h_{t-1}$. Chẳng hạn, $h_{t-1}$ lại được tính từ $x_{t-1}$ và $h_{t-2}$, và quá trình này tiếp tục ngược về các bước thời gian trước. Vì vậy, về mặt lý thuyết, trạng thái $h_t$ có khả năng tổng hợp thông tin từ chuỗi các quan sát $x_1,x_2,\dots,x_t$ và đóng vai trò như một dạng bộ nhớ của mạng tại thời điểm $t$.

Một đặc điểm quan trọng của RNN là cùng một bộ tham số $(W_h,U_h,b_h)$ được sử dụng tại tất cả các bước thời gian. Việc chia sẻ tham số này cho phép mạng xử lý các chuỗi có độ dài khác nhau mà không cần xây dựng một tập tham số riêng cho từng vị trí trong chuỗi. Khi biểu diễn dưới dạng khai triển theo thời gian (\textit{unrolled}), một RNN có thể được xem như một chuỗi các tế bào mạng có cùng cấu trúc và cùng trọng số, trong đó trạng thái ẩn được truyền tuần tự từ bước này sang bước kế tiếp.

\begin{figure}[H]
\centering
\begin{subfigure}[b]{0.22\textwidth}
\centering
\begin{tikzpicture}[baseline]
\node[draw, rounded corners, minimum width=1.3cm, minimum height=1cm, fill=blue!10] (A) {$A$};
\draw[cline] ($(A.east)+(0,-0.2)$) -- ++(0.5,0) -- ++(0,0.4) -- ($(A.east)+(0,0.2)$);
\draw[cline] ($(A.south)+(0,-0.9)$) node[below]{$x_t$} -- (A.south);
\draw[cline] (A.north) -- ++(0,0.9) node[above]{$h_t$};
\end{tikzpicture}
\caption{Dạng cuộn}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.74\textwidth}
\centering
\begin{tikzpicture}[baseline]
\node[draw, rounded corners, minimum width=1.2cm, minimum height=1cm, fill=blue!10] (A0) at (0,0) {$A$};
\node[draw, rounded corners, minimum width=1.2cm, minimum height=1cm, fill=blue!10] (A1) at (2.4,0) {$A$};
\node[draw, rounded corners, minimum width=1.2cm, minimum height=1cm, fill=blue!10] (A2) at (4.8,0) {$A$};
\draw[cline] ($(A0.south)+(0,-0.9)$) node[below]{$x_{t-1}$} -- (A0.south);
\draw[cline] ($(A1.south)+(0,-0.9)$) node[below]{$x_t$} -- (A1.south);
\draw[cline] ($(A2.south)+(0,-0.9)$) node[below]{$x_{t+1}$} -- (A2.south);
\draw[cline] (A0.north) -- ++(0,0.9) node[above]{$h_{t-1}$};
\draw[cline] (A1.north) -- ++(0,0.9) node[above]{$h_t$};
\draw[cline] (A2.north) -- ++(0,0.9) node[above]{$h_{t+1}$};
\draw[cline] (-1.1,0) -- (A0.west);
\draw[cline] (A0.east) -- (A1.west);
\draw[cline] (A1.east) -- (A2.west);
\draw[cline] (A2.east) -- ++(1.1,0);
\end{tikzpicture}
\caption{Dạng khai triển theo thời gian (\textit{unrolled})}
\end{subfigure}
\caption{Kiến trúc mạng nơ-ron hồi quy (RNN)}
\label{fig:rnn}
\end{figure}

Hình~\ref{fig:rnn} minh họa hai cách biểu diễn tương đương của một RNN. Ở dạng cuộn, kết nối hồi quy thể hiện việc trạng thái của tế bào mạng được truyền trở lại để sử dụng tại bước thời gian tiếp theo. Khi khai triển theo thời gian, cùng một tế bào $A$ được biểu diễn tại nhiều thời điểm liên tiếp. Tại mỗi bước, tế bào nhận đầu vào $x_t$ và trạng thái ẩn $h_{t-1}$ từ bước trước để tạo ra trạng thái mới $h_t$, sau đó $h_t$ tiếp tục được truyền sang bước kế tiếp. Các tế bào trong hình không phải là những mạng độc lập mà cùng chia sẻ một bộ tham số.

Mặc dù cơ chế hồi quy cho phép RNN về lý thuyết khai thác thông tin từ các quan sát trong quá khứ, RNN truyền thống gặp khó khăn khi cần học các mối quan hệ phụ thuộc cách xa nhau trong chuỗi. Nguyên nhân chủ yếu xuất hiện trong quá trình huấn luyện bằng \textbf{lan truyền ngược qua thời gian} (\textit{Backpropagation Through Time -- BPTT}). Theo quy tắc dây chuyền, ảnh hưởng của một trạng thái ở thời điểm $t-k$ đến trạng thái tại thời điểm $t$ được xác định thông qua tích của nhiều đạo hàm liên tiếp:

\begin{equation}
\frac{\partial h_t}{\partial h_{t-k}} = \prod_{j=t-k+1}^{t} \frac{\partial h_j}{\partial h_{j-1}}.
\end{equation}

Khi chuỗi dài, việc liên tục nhân các đạo hàm này có thể khiến gradient giảm rất nhanh về gần $0$, được gọi là hiện tượng \textbf{vanishing gradient}. Khi đó, thông tin từ những bước thời gian ở xa hầu như không còn đóng góp đáng kể vào quá trình cập nhật trọng số, khiến RNN khó học được các phụ thuộc dài hạn. Ngược lại, gradient cũng có thể tăng lên rất lớn qua nhiều bước, gây ra hiện tượng \textbf{exploding gradient}, làm quá trình huấn luyện trở nên không ổn định.

Những hạn chế trên, đặc biệt là vấn đề vanishing gradient khi xử lý các phụ thuộc dài hạn, là một trong những động lực chính dẫn đến sự phát triển của các kiến trúc RNN có cơ chế cổng như \textbf{Long Short-Term Memory (LSTM)} và sau đó là \textbf{Gated Recurrent Unit (GRU)}.

\section{Long Short-Term Memory (LSTM)}

Long Short-Term Memory (LSTM), được đề xuất bởi Hochreiter và Schmidhuber (1997), là một kiến trúc mở rộng của RNN nhằm cải thiện khả năng học các quan hệ phụ thuộc dài hạn trong dữ liệu chuỗi. So với RNN truyền thống, điểm khác biệt quan trọng của LSTM là việc bổ sung một \textbf{trạng thái cell} (\textit{cell state}) ký hiệu là $C_t$. Trạng thái này đóng vai trò như một kênh lưu trữ thông tin xuyên suốt theo trục thời gian, cho phép thông tin quan trọng được duy trì qua nhiều bước liên tiếp với ít biến đổi hơn. Nhờ đó, quá trình lan truyền gradient ngược qua thời gian trở nên ổn định hơn, từ đó làm giảm ảnh hưởng của hiện tượng vanishing gradient vốn là hạn chế điển hình của RNN nguyên bản.

Bên cạnh trạng thái cell, LSTM còn sử dụng các \textbf{cổng điều khiển} (\textit{gates}) để kiểm soát luồng thông tin đi vào, được giữ lại hay được đưa ra khỏi bộ nhớ. Mỗi cổng được xây dựng bằng hàm sigmoid, cho đầu ra trong khoảng $[0,1]$, nhờ đó có thể được hiểu như một cơ chế ``đóng -- mở'' để điều tiết mức độ truyền thông tin. Tại thời điểm $t$, LSTM nhận đầu vào hiện tại $x_t$ và trạng thái ẩn của bước trước $h_{t-1}$ để tính toán các đại lượng trung gian như sau.

Trước hết, \textbf{cổng quên} (\textit{forget gate}) xác định mức độ thông tin từ trạng thái cell trước đó $C_{t-1}$ cần được giữ lại:

\begin{equation}
f_t = \sigma(W_f x_t + U_f h_{t-1} + b_f)
\end{equation}

Trong đó, nếu một phần tử của $f_t$ có giá trị gần $1$ thì thông tin tương ứng trong $C_{t-1}$ gần như được giữ nguyên; ngược lại, nếu giá trị gần $0$ thì thông tin đó sẽ bị loại bỏ phần lớn.

Tiếp theo, \textbf{cổng vào} (\textit{input gate}) quyết định mức độ thông tin mới sẽ được ghi vào trạng thái cell:

\begin{equation}
i_t = \sigma(W_i x_t + U_i h_{t-1} + b_i)
\end{equation}

Song song với đó, mạng tính một \textbf{trạng thái cell ứng viên} thể hiện nội dung mới có thể được bổ sung vào bộ nhớ:

\begin{equation}
\tilde{C}_t = \tanh(W_C x_t + U_C h_{t-1} + b_C)
\end{equation}

Từ hai thành phần trên, trạng thái cell tại thời điểm $t$ được cập nhật bằng cách kết hợp thông tin cũ cần giữ lại với thông tin mới cần bổ sung:

\begin{equation}
C_t = f_t \odot C_{t-1} + i_t \odot \tilde{C}_t
\end{equation}

Trong công thức này, ký hiệu $\odot$ biểu diễn phép nhân theo từng phần tử. Thành phần $f_t \odot C_{t-1}$ cho biết phần thông tin cũ còn được duy trì, trong khi $i_t \odot \tilde{C}_t$ biểu diễn phần thông tin mới được thêm vào bộ nhớ.

Sau khi trạng thái cell mới $C_t$ được hình thành, \textbf{cổng ra} (\textit{output gate}) sẽ quyết định phần nào của thông tin trong cell state được sử dụng để tạo ra trạng thái ẩn tại thời điểm hiện tại:

\begin{equation}
o_t = \sigma(W_o x_t + U_o h_{t-1} + b_o)
\end{equation}

\begin{equation}
h_t = o_t \odot \tanh(C_t)
\end{equation}

Như vậy, trạng thái ẩn $h_t$ không phải là toàn bộ nội dung của $C_t$, mà là phần thông tin đã được ``lọc'' qua cổng ra. Trạng thái này vừa đóng vai trò đầu ra của ô nhớ tại thời điểm $t$, vừa được truyền sang bước thời gian tiếp theo cùng với trạng thái cell $C_t$.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/LSTM_structure.pdf}
\caption{Kiến trúc LSTM cell (nguồn: Zhang và cộng sự, \textit{Dive into Deep Learning})}
\label{fig:lstm}
\end{figure}

Hình~\ref{fig:lstm} minh họa luồng thông tin trong một cell LSTM: đường ngang phía trên (cell state $\mathbf{C}_t$) chỉ tương tác với phần còn lại của mạng qua hai phép toán đơn giản (nhân theo phần tử với $\mathbf{F}_t$, cộng với $\mathbf{I}_t \odot \tilde{\mathbf{C}}_t$) — gần như không đi qua hàm phi tuyến nào, giúp gradient lan truyền ngược tốt hơn. Bốn cổng $\mathbf{F}_t, \mathbf{I}_t, \tilde{\mathbf{C}}_t, \mathbf{O}_t$ đều nhận đầu vào là $\mathbf{H}_{t-1}$ nối với $\mathbf{X}_t$; trạng thái ẩn $\mathbf{H}_t$ (không vẽ trong hình, xem công thức ở trên) được tính riêng bằng cách đưa $\mathbf{C}_t$ qua $\tanh(\cdot)$ rồi nhân theo phần tử với cổng ra $\mathbf{O}_t$. Với ba cổng độc lập và một cell state riêng biệt, LSTM có nhiều tham số hơn RNN nhưng giải quyết hiệu quả vấn đề vanishing gradient, trở thành kiến trúc RNN có cổng được sử dụng rộng rãi nhất trong nhiều năm.

\section{Gated Recurrent Unit (GRU)}

Gated Recurrent Unit (GRU), được đề xuất bởi Cho và cộng sự (2014), là một kiến trúc mạng nơ-ron hồi quy có cổng được xây dựng nhằm cải thiện khả năng học các phụ thuộc dài hạn của RNN truyền thống. Tương tự LSTM, GRU sử dụng các cổng điều khiển để điều tiết luồng thông tin qua thời gian, nhưng có cấu trúc gọn hơn do không duy trì trạng thái ô nhớ $C_t$ riêng biệt và chỉ sử dụng hai cổng chính: \textbf{cổng đặt lại} (\textit{reset gate}) và \textbf{cổng cập nhật} (\textit{update gate}).

Tại thời điểm $t$, với đầu vào $x_t$ và trạng thái ẩn trước đó $h_{t-1}$, cổng đặt lại được xác định bởi:

\begin{equation}
r_t = \sigma(W_r x_t + U_r h_{t-1} + b_r),
\end{equation}

trong đó $r_t$ kiểm soát mức độ thông tin từ $h_{t-1}$ được sử dụng khi xây dựng trạng thái ứng viên. Khi $r_t$ có giá trị gần $0$, ảnh hưởng của thông tin quá khứ tương ứng được giảm bớt; ngược lại, giá trị gần $1$ cho phép thông tin đó được giữ lại nhiều hơn.

Cổng cập nhật được tính theo:

\begin{equation}
z_t = \sigma(W_z x_t + U_z h_{t-1} + b_z),
\end{equation}

và có nhiệm vụ điều tiết tỷ lệ giữa việc duy trì trạng thái ẩn trước đó và tiếp nhận thông tin mới.

Dựa trên cổng đặt lại, trạng thái ẩn ứng viên được xác định bởi:

\begin{equation}
\tilde{h}_t = \tanh\left( W_h x_t + U_h(r_t \odot h_{t-1}) + b_h \right),
\end{equation}

trong đó $\odot$ biểu diễn phép nhân theo từng phần tử. Trạng thái ẩn tại thời điểm $t$ sau đó được cập nhật theo:

\begin{equation}
h_t = z_t \odot h_{t-1} + (1-z_t)\odot\tilde{h}_t.
\end{equation}

Theo quy ước trên, khi $z_t$ tiến gần $1$, trạng thái trước đó $h_{t-1}$ được giữ lại nhiều hơn; ngược lại, khi $z_t$ tiến gần $0$, trạng thái mới chủ yếu được xác định bởi trạng thái ứng viên $\tilde{h}_t$. Cơ chế này cho phép GRU duy trì thông tin quan trọng qua nhiều bước thời gian, qua đó hỗ trợ việc học các phụ thuộc dài hạn trong dữ liệu tuần tự.

\begin{figure}[H]
\centering
\includegraphics[width=0.7\textwidth]{figures/GRU_structure.pdf}
\caption{Kiến trúc GRU: tính toán Reset gate, Update gate và Trạng thái ẩn ứng viên (nguồn: Zhang và cộng sự, \textit{Dive into Deep Learning})}
\label{fig:gru}
\end{figure}

Hình~\ref{fig:gru} minh họa bước tính Reset gate $\mathbf{R}_t$, Update gate $\mathbf{Z}_t$ và trạng thái ẩn ứng viên $\tilde{\mathbf{H}}_t$: khác với LSTM, GRU không có đường cell state riêng — $\mathbf{H}_{t-1}$ được "sao chép" thành hai nhánh, một đi thẳng vào hai cổng $\mathbf{R}_t, \mathbf{Z}_t$ (cùng $\mathbf{X}_t$), một được nhân theo phần tử với $\mathbf{R}_t$ trước khi cùng $\mathbf{X}_t$ tạo thành trạng thái ứng viên $\tilde{\mathbf{H}}_t$ qua $\tanh(\cdot)$. Trạng thái ẩn cuối cùng $\mathbf{H}_t$ (không vẽ trong hình, xem công thức ở trên) chỉ là bước nội suy tuyến tính đơn giản giữa $\mathbf{H}_{t-1}$ và $\tilde{\mathbf{H}}_t$ theo hệ số $\mathbf{Z}_t$, không cần thêm phép biến đổi phi tuyến nào. Khi $z_t \to 1$, trạng thái ẩn gần như được giữ nguyên qua bước thời gian — cơ chế then chốt giúp GRU giảm thiểu vanishing gradient tương tự cell state của LSTM; khi $z_t \to 0$, trạng thái được thay gần như hoàn toàn bằng trạng thái ứng viên mới.

So với LSTM, việc sử dụng một trạng thái ẩn duy nhất và hai cổng điều khiển giúp GRU có cấu trúc gọn hơn và thường có ít tham số hơn, trong khi vẫn duy trì khả năng mô hình hóa các phụ thuộc dài hạn tốt hơn RNN truyền thống.

Với bài toán dự báo hồi quy (không phải phân loại) tại một horizon $h$ cố định, cho trước ($h \geq 1$ là số bước thời gian cần dự báo trước, được chọn trước khi huấn luyện), đầu ra cuối cùng của mô hình là trạng thái ẩn tại bước thời gian cuối của cửa sổ đầu vào, đưa qua một lớp fully-connected tuyến tính để cho ra giá trị dự báo của biến mục tiêu tại bước $t+h$:
\begin{equation}
\hat{y}_{t+h} = W_{fc}\, h_{w} + b_{fc},
\end{equation}
với $h_w$ là trạng thái ẩn tại bước cuối cùng ($w$) của cửa sổ đầu vào. Horizon $h$ ở đây là một \textbf{siêu tham số kiến trúc}: mỗi model chỉ được huấn luyện để dự báo đúng một giá trị $h$ duy nhất — muốn đổi sang horizon khác phải huấn luyện lại từ đầu.

Trong thực tế, nhiều bài toán cần dự báo \textbf{đồng thời nhiều horizon khác nhau} (chẳng hạn cả $1, 2, \dots, H$ bước tới cùng lúc), thay vì chỉ một $h$ duy nhất như kiến trúc cơ sở ở trên. Hai chiến lược phổ biến để mở rộng một model một-horizon sang dự báo $H$ bước tương lai được trình bày dưới đây.

\subsection*{Recursive GRU}

Một cách tiếp cận đơn giản để thực hiện dự báo nhiều bước tương lai mà không cần huấn luyện một mô hình mới là sử dụng lặp lại mô hình GRU dự báo một bước ($h=1$) đã được huấn luyện trước. Chiến lược này được gọi là \textbf{recursive forecasting}. Ý tưởng cốt lõi là sau khi mô hình dự báo giá trị tại thời điểm $t+1$, giá trị dự báo đó sẽ được đưa ngược trở lại chuỗi đầu vào để tiếp tục dự báo cho thời điểm $t+2$, và quá trình này được lặp lại cho đến khi đạt horizon mong muốn $H$.

Cụ thể, tại bước dự báo thứ $k$ ($k=1,\dots,H$), mô hình tạo ra dự báo:

\begin{equation}
\hat{y}_{t+k} = f_{\theta}\big(\tilde{x}_{t-w+k}, \dots, \tilde{x}_{t-1+k}\big),
\end{equation}

trong đó $f_{\theta}$ là mô hình GRU dự báo một bước đã được huấn luyện, còn $\tilde{x}_i$ là vector đặc trưng tại thời điểm $i$. Nếu $i \leq t$, ta có $\tilde{x}_i = x_i$, tức là sử dụng dữ liệu quan trắc thực tế. Nếu $i > t$, thành phần mục tiêu trong $\tilde{x}_i$ được thay thế bằng chính giá trị dự báo trước đó $\hat{y}_i$. Đối với các đặc trưng ngoại sinh chưa có giá trị trong tương lai, một lựa chọn thực hành phổ biến là sử dụng giả định \textit{persistence}, tức giữ nguyên giá trị cuối cùng đã biết. Riêng các đặc trưng thời gian có tính xác định, chẳng hạn như giờ trong ngày hoặc ngày trong tuần được mã hóa tuần hoàn, có thể được tính trực tiếp cho các thời điểm tương lai.

\begin{figure}[H]
\centering
\begin{tikzpicture}[>=Latex, x=1cm, y=1cm]
  \node[realcell] at (0,4.3) {};
  \node[right, font=\scriptsize] at (0.5,4.3) {Dữ liệu thật};
  \node[predcell] at (3.2,4.3) {};
  \node[right, font=\scriptsize] at (3.7,4.3) {Model tự dự đoán};

  \node[font=\small\bfseries] at (-2.3,3) {Bước 1};
  \foreach \i in {0,1,2,3} {
    \node[realcell] (a\i) at (\i*0.9,3) {};
  }
  \node[grubox] (g1) at (4.6,3) {GRU};
  \draw[cline] (a3.east) -- (g1.west);
  \draw[cline] (g1.east) -- ++(0.9,0) node[predcell,right=0.05cm,font=\scriptsize] (p1) {$\hat y_{w+1}$};

  \node[font=\small\bfseries] at (-2.3,1.6) {Bước 2};
  \foreach \i in {0,1,2} {
    \node[realcell] (b\i) at (\i*0.9,1.6) {};
  }
  \node[predcell] (b3) at (2.7,1.6) {$\hat y_{w+1}$};
  \node[grubox] (g2) at (4.6,1.6) {GRU};
  \draw[cline] (b3.east) -- (g2.west);
  \draw[cline] (g2.east) -- ++(0.9,0) node[predcell,right=0.05cm,font=\scriptsize] (p2) {$\hat y_{w+2}$};

  \node[font=\small\bfseries] at (-2.3,0.2) {Bước 3};
  \node[realcell] (c0) at (0,0.2) {};
  \node[realcell] (c1) at (0.9,0.2) {};
  \node[predcell] (c2) at (1.8,0.2) {$\hat y_{w+1}$};
  \node[predcell] (c3) at (2.7,0.2) {$\hat y_{w+2}$};
  \node[grubox] (g3) at (4.6,0.2) {GRU};
  \draw[cline] (c3.east) -- (g3.west);
  \draw[cline] (g3.east) -- ++(0.9,0) node[predcell,right=0.05cm,font=\scriptsize] (p3) {$\hat y_{w+3}$};

  \draw[-{Latex[length=1.5mm]}, gray, dashed] (p1.south) to[out=-90,in=90] (b3.north);
  \draw[-{Latex[length=1.5mm]}, gray, dashed] (p2.south) to[out=-90,in=90] (c3.north);
\end{tikzpicture}
\caption{Recursive GRU: giá trị dự báo ở bước trước được đưa ngược trở lại làm đầu vào cho bước sau}
\label{fig:recursive}
\end{figure}

Hình~\ref{fig:recursive} minh họa cơ chế recursive qua ba bước đầu tiên. Ở Bước 1, cửa sổ đầu vào hoàn toàn gồm dữ liệu thực tế và mô hình tạo ra dự báo đầu tiên. Sang Bước 2, giá trị vừa dự báo được đưa vào cửa sổ đầu vào thay cho quan sát cũ nhất đã bị loại ra do cửa sổ trượt. Đến Bước 3, cửa sổ đầu vào đã chứa hai giá trị dự báo từ các bước trước. Như vậy, khi số bước dự báo tăng lên, số lượng giá trị dự báo được đưa ngược vào đầu vào cũng tăng dần, trong khi tỷ lệ dữ liệu thực tế trong cửa sổ giảm xuống.

Ưu điểm của chiến lược này là tận dụng trực tiếp mô hình dự báo một bước đã có sẵn mà không cần huấn luyện một mô hình riêng cho từng horizon. Tuy nhiên, hạn chế chính là hiện tượng \textbf{tích lũy sai số} (\textit{error compounding}): sai số lan dần qua các bước dự báo, khiến độ tin cậy giảm khi horizon càng xa.

\subsection*{Direct Multi-output GRU}

Một hạn chế quan trọng của chiến lược Recursive là các giá trị dự đoán ở những bước trước được đưa trở lại làm đầu vào cho những bước sau. Do đó, sai số xuất hiện ở các horizon gần có thể tiếp tục lan truyền và ảnh hưởng đến các dự báo xa hơn. Chiến lược Direct Multi-output giải quyết vấn đề này bằng cách thay đổi cách xây dựng đầu ra của mô hình: thay vì dự báo từng bước một cách tuần tự, mô hình được huấn luyện để dự báo trực tiếp toàn bộ $H$ bước tương lai trong một lần forward pass.

Với một cửa sổ lịch sử có độ dài $w$ kết thúc tại thời điểm $t$,

\[
X_{t-w+1:t}
=
[x_{t-w+1},x_{t-w+2},\ldots,x_t],
\]

Encoder GRU lần lượt xử lý các quan sát trong cửa sổ và tạo ra trạng thái ẩn cuối cùng $h_w$. Trạng thái này có thể được xem như một biểu diễn cô đọng của toàn bộ thông tin lịch sử mà mô hình quan sát được trước thời điểm dự báo.

Khác với mô hình one-step GRU, trong đó lớp đầu ra chỉ sinh một giá trị,

\[
\hat y_{t+1}
=
W_{fc}h_w+b_{fc},
\]

Direct Multi-output GRU sử dụng một lớp fully-connected (Linear layer) có $H$ nút đầu ra. Lớp Linear thực hiện phép biến đổi

\[
z = W_{fc}h_w + b_{fc},
\]

trong đó $W_{fc}$ và $b_{fc}$ là các tham số được học trong quá trình huấn luyện. Nếu hidden state có số chiều là $d_h$, lớp đầu ra có dạng

\[
\text{Linear}(d_h,H).
\]

Do đó, từ cùng một trạng thái ẩn $h_w$, mô hình sinh trực tiếp một vector gồm toàn bộ $H$ giá trị cần dự báo:

\begin{equation}
\begin{bmatrix}
\hat y_{t+1}\\
\hat y_{t+2}\\
\vdots\\
\hat y_{t+H}
\end{bmatrix}
=
W_{fc}h_w+b_{fc},
\qquad
W_{fc}\in\mathbb{R}^{H\times d_h},
\qquad
b_{fc}\in\mathbb{R}^{H}.
\end{equation}

Có thể xem mỗi hàng của ma trận $W_{fc}$ là một ánh xạ riêng ứng với một horizon dự báo:

\[
\hat y_{t+k}
=
w^{(k)}h_w+b_k,
\qquad
k=1,2,\ldots,H.
\]

Như vậy, mặc dù tất cả các horizon đều sử dụng chung biểu diễn lịch sử $h_w$, lớp Linear vẫn học một bộ trọng số đầu ra riêng cho từng bước dự báo.

Trong quá trình huấn luyện, mỗi mẫu dữ liệu không còn có một target đơn lẻ mà có một vector target:

\[
Y_t
=
[y_{t+1},y_{t+2},\ldots,y_{t+H}].
\]

Nếu sử dụng Mean Squared Error, hàm mất mát có thể được tính đồng thời trên toàn bộ horizon:

\begin{equation}
\mathcal{L}
=
\frac{1}{H}
\sum_{k=1}^{H}
\left(
y_{t+k}-\hat y_{t+k}
\right)^2.
\end{equation}

Nhờ đó, các tham số của Encoder GRU và lớp đầu ra được tối ưu đồng thời để trạng thái $h_w$ chứa thông tin hữu ích cho cả các dự báo gần lẫn các dự báo xa.

\begin{figure}[H]
\centering

\begin{tikzpicture}[>=Latex, x=1cm, y=1cm]

    \foreach \i in {0,1,2,3} {
        \node[realcell] (a\i) at (\i*0.9,0) {};
    }

    \node[font=\scriptsize] at (1.35,-0.6)
    {Cửa sổ lịch sử $X_{t-w+1:t}$};

    \node[grubox, minimum width=1.6cm, minimum height=1cm]
    (enc) at (5.2,0) {Encoder GRU};

    \draw[cline] (a3.east) -- (enc.west);

    \node[opnode, minimum size=0.5cm]
    (hw) at (7.3,0) {$h_w$};

    \draw[cline] (enc.east) -- (hw.west);

    \node[gatebox, fill=violet!15, minimum width=1.7cm]
    (fc) at (9.4,0) {Linear$(d_h,H)$};

    \draw[cline] (hw.east) -- (fc.west);

    \node[predcell] (o1) at (11.6,1.65) {$\hat y_{t+1}$};
    \node[predcell] (o2) at (11.6,0.55) {$\hat y_{t+2}$};
    \node[font=\small] (odots) at (11.6,-0.55) {$\vdots$};
    \node[predcell] (oH) at (11.6,-1.65) {$\hat y_{t+H}$};

    \foreach \o in {o1,o2,oH} {
        \draw[cline] (fc.east) -- (\o.west);
    }

    \node[font=\scriptsize] at (9.4,-2.6)
    {Một forward pass cho toàn bộ $H$ horizon};

\end{tikzpicture}

\caption{Direct Multi-output GRU: Encoder GRU mã hóa cửa sổ lịch sử thành trạng thái ẩn cuối $h_w$, sau đó lớp Linear sinh đồng thời toàn bộ $H$ giá trị dự báo.}

\label{fig:direct}
\end{figure}

Hình~\ref{fig:direct} minh họa sự khác biệt cốt lõi giữa chiến lược Direct Multi-output và Recursive. Tại thời điểm dự báo $t$, mô hình sử dụng cửa sổ dữ liệu lịch sử quan sát được $X_{t-w+1:t}$ để tạo ra trạng thái ẩn $h_w$. Từ trạng thái này, toàn bộ các giá trị
$\hat y_{t+1},\hat y_{t+2},\ldots,\hat y_{t+H}$
được sinh đồng thời trong một forward pass.

Điểm quan trọng là không có dự đoán nào được đưa trở lại làm đầu vào để tạo ra dự đoán tiếp theo. Chẳng hạn, $\hat y_{t+2}$ không được tính từ $\hat y_{t+1}$, và $\hat y_{t+3}$ cũng không cần sử dụng $\hat y_{t+2}$. Vì vậy, khác với Recursive GRU, sai số tại một horizon không trực tiếp lan truyền sang các horizon phía sau thông qua đầu vào của mô hình.

Tuy nhiên, các đầu ra không hoàn toàn độc lập với nhau. Tất cả chúng đều được tạo từ cùng một biểu diễn lịch sử $h_w$ và cùng được tối ưu trong quá trình huấn luyện. Do đó, chính xác hơn nên nói rằng các horizon \textit{không phụ thuộc đệ quy vào nhau}, thay vì nói rằng chúng hoàn toàn độc lập.

\textbf{Ưu điểm.}
Ưu điểm chính của Direct Multi-output GRU là loại bỏ cơ chế lan truyền sai số đặc trưng của dự báo Recursive. Do không sử dụng các giá trị dự đoán trước đó làm đầu vào, một sai số lớn tại $\hat y_{t+1}$ không trực tiếp làm thay đổi đầu vào dùng để tính $\hat y_{t+2}$ hoặc các horizon xa hơn. Bên cạnh đó, toàn bộ $H$ bước được sinh trong một forward pass nên quá trình suy luận thường nhanh hơn so với việc gọi mô hình $H$ lần liên tiếp như trong Recursive forecasting.

\textbf{Nhược điểm.}
Đổi lại, mô hình phải học đồng thời ánh xạ từ cùng một biểu diễn lịch sử sang nhiều horizon khác nhau. Ngoài ra, kiến trúc đầu ra phụ thuộc trực tiếp vào giá trị $H$. Chẳng hạn, một mô hình được huấn luyện với $H=6$ sẽ có lớp đầu ra $\text{Linear}(d_h,6)$ và sinh trực tiếp sáu giá trị tương ứng với các thời điểm từ $t+1$ đến $t+6$. Nếu muốn thay đổi horizon dự báo sang $H=12$, cần thay đổi kích thước lớp đầu ra thành $\text{Linear}(d_h,12)$ và thông thường phải huấn luyện lại mô hình.

% ==================== CHƯƠNG 2: ỨNG DỤNG ====================
\chapter{Ứng dụng dự báo nồng độ PM2.5}

\section{Mô tả dữ liệu}

Bộ dữ liệu gốc (\texttt{raw\_data.csv}) được tải trực tiếp từ Open-Meteo Air Quality API, trải dài từ năm 2016 nhưng phần lớn giai đoạn đầu (2016 -- giữa 2022) không có dữ liệu (toàn bộ giá trị NaN) do trạm quan trắc/API chưa cung cấp dữ liệu ở giai đoạn đó. Sau bước làm sạch (loại bỏ các hàng toàn NaN), bộ dữ liệu sạch (\texttt{cleaned\_data.csv}) còn lại \textbf{34.865 dòng $\times$ 15 cột}, liên tục theo từng giờ từ \texttt{2022-08-04 07:00:00} đến \texttt{2026-07-26 23:00:00}, không còn giá trị thiếu.

Các biến trong bộ dữ liệu sau khi chuẩn hóa tên cột:

\begin{itemize}
    \item \textbf{PM10}: Nồng độ bụi mịn đường kính $\leq 10\,\mu$m ($\mu$g/m$^3$)
    \item \textbf{PM2.5}: Nồng độ bụi mịn đường kính $\leq 2.5\,\mu$m ($\mu$g/m$^3$) — \textit{biến mục tiêu}
    \item \textbf{Carbon Monoxide (CO)}: Khí carbon monoxide ($\mu$g/m$^3$)
    \item \textbf{Nitrogen Dioxide (NO$_2$)}: Khí nitơ đioxit ($\mu$g/m$^3$)
    \item \textbf{Sulphur Dioxide (SO$_2$)}: Khí lưu huỳnh đioxit ($\mu$g/m$^3$)
    \item \textbf{Ozone (O$_3$)}: Khí ozone tầng mặt đất ($\mu$g/m$^3$)
    \item \textbf{Aerosol Optical Depth (AOD)}: Độ dày quang học sol khí, phản ánh mức độ khí quyển bị che khuất bởi hạt lơ lửng (không đơn vị)
    \item \textbf{Dust}: Nồng độ bụi ($\mu$g/m$^3$)
    \item \textbf{UV Index}, \textbf{UV Index Clear Sky}: Chỉ số tia UV thực tế và giả định trời quang mây (không đơn vị)
    \item \textbf{day, month, year, hour}: Các thành phần thời gian tách từ cột \texttt{time}
\end{itemize}

\section{Phân tích khám phá dữ liệu (EDA)}

\begin{table}[H]
\centering
\caption{Thống kê mô tả các biến định lượng}
\label{tab:describe}
\begin{tabular}{lrrrrrr}
\toprule
Biến & Đơn vị & Mean & Std & Min & Median & Max \\
\midrule
PM10 & $\mu$g/m$^3$ & 19.76 & 9.91 & 0.40 & 18.20 & 97.80 \\
PM2.5 & $\mu$g/m$^3$ & 13.73 & 7.06 & 0.30 & 12.50 & 71.50 \\
Carbon Monoxide & $\mu$g/m$^3$ & 249.38 & 109.28 & 59.00 & 226.00 & 1185.00 \\
Nitrogen Dioxide & $\mu$g/m$^3$ & 3.05 & 2.59 & 0.00 & 2.30 & 29.30 \\
Sulphur Dioxide & $\mu$g/m$^3$ & 2.23 & 1.37 & 0.10 & 1.90 & 13.50 \\
Ozone & $\mu$g/m$^3$ & 81.58 & 30.30 & 4.00 & 79.00 & 259.00 \\
Aerosol Optical Depth & --- & 0.25 & 0.16 & 0.02 & 0.21 & 2.37 \\
Dust & $\mu$g/m$^3$ & 0.42 & 1.45 & 0.00 & 0.00 & 37.00 \\
UV Index & --- & 2.01 & 3.21 & 0.00 & 0.00 & 13.65 \\
UV Index Clear Sky & --- & 2.53 & 3.84 & 0.00 & 0.00 & 14.10 \\
\bottomrule
\end{tabular}
\end{table}

Ba biến cuối (\textit{Dust}, \textit{UV Index}, \textit{UV Index Clear Sky}) có trung vị bằng 0, cho thấy phân phối lệch phải rất mạnh với phần lớn giá trị bằng 0 — dấu hiệu cho thấy các biến này có thể ít mang thông tin hữu ích cho việc dự báo PM2.5, sẽ được kiểm chứng qua phân tích tương quan ở phần tiếp theo.

\subsection*{Tương quan giữa các chỉ số chất lượng không khí}

\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{figures/correlation_heatmap.png}
\caption{Ma trận tương quan Pearson giữa các chỉ số chất lượng không khí}
\label{fig:corr}
\end{figure}

Hình~\ref{fig:corr} cho thấy PM10 và PM2.5 có tương quan rất cao ($r = 0.91$) — phù hợp với thực tế vì PM2.5 là tập con của PM10. PM2.5 cũng có tương quan trung bình với Carbon Monoxide ($r=0.50$), Nitrogen Dioxide ($r=0.44$) và Aerosol Optical Depth ($r=0.55$) — các chỉ số phần lớn liên quan đến hoạt động đốt cháy (giao thông, công nghiệp). Ngược lại, Dust ($r=0.10$) và UV Index/UV Index Clear Sky ($r \approx -0.01$ đến $-0.03$) gần như không tương quan với PM2.5 — nhất quán với nhận xét về phân phối lệch ở trên.

\subsection*{Xu hướng và phân phối theo thời gian}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/pm25_lineplot.png}
\caption{Chuỗi thời gian nồng độ PM2.5 (04/08/2022 -- 26/07/2026)}
\label{fig:lineplot}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.7\textwidth]{figures/pm25_histogram.png}
\caption{Phân phối PM2.5: Histogram và ước lượng mật độ (KDE)}
\label{fig:hist}
\end{figure}

Chuỗi PM2.5 (Hình~\ref{fig:lineplot}) dao động chủ yếu trong khoảng 5--30~$\mu$g/m$^3$, với các đợt tăng đột biến ngắn hạn (lên đến $\sim$70~$\mu$g/m$^3$). Tính chu kỳ theo mùa thể hiện qua việc mật độ và biên độ các đợt tăng đột biến không trải đều theo thời gian mà tập trung thành từng "dải" rõ rệt, lặp lại đều đặn mỗi năm một lần vào khoảng tháng 1--5 (dễ thấy nhất ở các giai đoạn đầu 2023, đầu 2025 và đầu 2026 trên Hình~\ref{fig:lineplot}), xen kẽ những giai đoạn nền thấp và ổn định hơn vào khoảng tháng 6--12. Phân phối PM2.5 (Hình~\ref{fig:hist}) lệch phải rõ rệt: đỉnh mật độ (mode) nằm ở khoảng 9--10~$\mu$g/m$^3$, thấp hơn cả trung vị (12.5) lẫn trung bình (13.73) — đúng thứ tự mode $<$ median $<$ mean đặc trưng của phân phối lệch phải. Từ mốc 0, mật độ tăng rất dốc lên đỉnh trong khoảng 0--10~$\mu$g/m$^3$, sau đó giảm chậm dần thành một đuôi phải kéo dài đến gần 100~$\mu$g/m$^3$ nhưng mật độ rất thấp (gần như phẳng sau mốc 40~$\mu$g/m$^3$) — phù hợp với đặc điểm dữ liệu môi trường, nơi phần lớn quan sát tập trung quanh nền ô nhiễm thông thường, còn các sự kiện ô nhiễm cao là thiểu số nhưng có biên độ lớn, kéo dài đuôi phân phối về phía bên phải.

\subsection*{Tính chu kỳ trong ngày (ACF/PACF)}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/acf_pacf.png}
\caption{ACF và PACF của PM2.5 theo giờ (168 lag $=$ 1 tuần)}
\label{fig:acfpacf}
\end{figure}

Biểu đồ ACF (Hình~\ref{fig:acfpacf}, đồ thị phía trên) giảm dần theo dạng dao động tắt dần: từ đỉnh tại lag 0 ($=1$), giảm xuống đáy cục bộ quanh lag 12 ($\approx 0.5$), rồi tăng trở lại thành đỉnh cục bộ tại lag 24 ($\approx 0.67$) — và lặp lại đúng kiểu dao động này tại các lag 48, 72, 96, 120, 144, 168, tức các bội số của 24 giờ. Xen giữa các đỉnh đó là các đáy cục bộ tại lag 36, 60, 84, 108, 132, 156 — đúng các mốc lệch pha nửa ngày (12 giờ) so với đỉnh gần nhất — với giá trị giảm dần từ $\approx 0.5$ (lag 12) xuống còn $\approx 0.18$ (lag 156), luôn thấp hơn rõ rệt so với đỉnh liền kề. Nói cách khác, PM2.5 tại một thời điểm tương quan yếu nhất với chính nó sau mỗi 12 giờ (nửa chu kỳ) và mạnh nhất sau mỗi 24 giờ (trọn một chu kỳ). Toàn bộ đường bao ACF suy giảm chậm từ 1.0 xuống còn khoảng 0.2--0.3 sau 168 giờ (1 tuần) nhưng không rơi xuống dưới 0, cho thấy PM2.5 có tính tự tương quan mạnh xuyên suốt cả tuần, với chu kỳ ngày (24 giờ) chồng lên trên xu hướng suy giảm chung đó.

Biểu đồ PACF (Hình~\ref{fig:acfpacf}, đồ thị phía dưới) bổ sung thêm bằng chứng: sau khi loại bỏ ảnh hưởng gián tiếp qua các lag trung gian, chỉ hai lag đầu tiên có hệ số tương quan riêng phần đáng kể — lag 1 ($\approx 0.97$) và lag 2 ($\approx -0.38$) — phản ánh PM2.5 tại một giờ phụ thuộc mạnh vào giá trị ngay trước đó, kèm một hiệu ứng điều chỉnh ngược ở giờ liền kề tiếp theo. Ngoài hai lag này, PACF gần như bằng 0 ở hầu hết các lag, \textbf{ngoại trừ} đúng những lag là bội số của 24 giờ (24, 48, 72,..., 144), nơi xuất hiện các hệ số âm nhỏ nhưng rõ rệt (từ $\approx -0.25$ tại lag 24, giảm dần về $\approx -0.05$ ở các lag xa hơn). Vì PACF đã loại bỏ tương quan gián tiếp qua các lag trung gian, các đỉnh âm này xác nhận chu kỳ ngày là một thành phần chu kỳ thật sự trong dữ liệu, chứ không chỉ là hệ quả của việc ACF suy giảm chậm.

Cả hai biểu đồ đều xác nhận \textbf{chu kỳ ngày (24 giờ)} là chu kỳ chi phối hành vi ngắn hạn của PM2.5, phù hợp với quy luật sinh hoạt/giao thông đô thị (giờ cao điểm) lặp lại mỗi ngày. Đây là một trong những căn cứ cho quyết định gộp dữ liệu về mức trung bình ngày trước khi đưa vào mô hình (Mục~2.3).

\subsection*{Xu hướng dài hạn và biến động theo mùa}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/pm25_moving_average.png}
\caption{PM2.5 và đường trung bình trượt (Moving Average) 7, 30 và 90 ngày}
\label{fig:ma}
\end{figure}

Hình~\ref{fig:ma} cho thấy các đường trung bình trượt 30 ngày và 90 ngày dao động lặp lại theo chu kỳ hàng năm — tăng vào giai đoạn đầu năm (khoảng tháng 1--4) và giảm dần về giữa năm — chứ không trôi dạt theo một hướng cố định qua các năm 2022--2026. Chuỗi giá trị thực tế (đường xám) dao động rất mạnh quanh các đường trung bình trượt, phản ánh nhiều đợt tăng đột biến ngắn hạn xen kẽ nền chu kỳ mùa; đặc điểm "không trôi dạt, chỉ dao động quanh một nền tương đối ổn định" này sẽ được thấy lại rõ hơn ở thành phần Trend khi phân rã chuỗi thời gian ngay sau đây.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/decomposition.png}
\caption{Phân rã chuỗi thời gian PM2.5 theo chu kỳ năm (Trend + Seasonal + Residual)}
\label{fig:decomp}
\end{figure}

Hình~\ref{fig:decomp} trình bày kết quả phân rã cổ điển áp dụng trên chuỗi PM2.5 trung bình ngày, tách thành bốn thành phần theo thứ tự từ trên xuống: giá trị thực tế (Observed), Trend, Seasonal và Residual.

Biểu đồ giá trị thực tế lặp lại chính chuỗi đã thấy ở Hình~\ref{fig:lineplot}, dao động phổ biến trong khoảng 5--25~$\mu$g/m$^3$ và xen kẽ những đợt tăng vọt lên đến 35--40~$\mu$g/m$^3$ vào một vài thời điểm (đầu năm 2023, đầu năm 2025, giữa năm 2026) — các cực trị này rơi đúng vào giai đoạn đầu năm mà đường trung bình trượt ở Hình~\ref{fig:ma} đã chỉ ra là cao điểm ô nhiễm hàng năm.

Thành phần Trend là một đường cong trơn, biến thiên rất chậm và chỉ trong biên độ hẹp 12--14.7~$\mu$g/m$^3$: giảm dần từ khoảng 14.1 (giữa năm 2022) xuống đáy $\approx 11.9$ (cuối năm 2023), sau đó tăng trở lại lên đỉnh $\approx 14.7$ (đầu năm 2025), rồi lại giảm về $\approx 13.1$ (cuối năm 2025) trước khi hồi phục nhẹ về cuối chuỗi quan sát. Đường Trend không tăng hay giảm đơn điệu mà chỉ dao động lên xuống quanh một nền chung khoảng 13~$\mu$g/m$^3$ — cho thấy PM2.5 tại khu vực khảo sát không có dấu hiệu ô nhiễm ngày càng nghiêm trọng (hay cải thiện) rõ rệt qua các năm, mà chỉ trải qua những đợt lên/xuống chậm, biên độ nhỏ, nằm ngoài phạm vi chu kỳ năm và chưa đủ cơ sở để coi là một xu hướng dài hạn thật sự.

Ngược lại, thành phần Seasonal dao động trong khoảng $-8$ đến $+13$ và lặp lại đều đặn theo từng năm: đạt đỉnh dương (khoảng $+10$ đến $+13$) vào giai đoạn tháng 1--4, rồi xuống đáy âm (khoảng $-5$ đến $-8$) vào giai đoạn giữa năm (tháng 6--9) — hoàn toàn nhất quán với quy luật "tăng đầu năm, giảm giữa năm" đã quan sát được ở Hình~\ref{fig:ma} và sẽ còn thấy lại ở Seasonal Subseries Plot (Hình~\ref{fig:seasonal}) ngay sau đây. Đáng chú ý, biên độ dao động của thành phần Seasonal (xấp xỉ $\pm 13$) lớn hơn nhiều so với biên độ của Trend (chưa đến 3 đơn vị) — khẳng định tính chu kỳ theo năm mới là yếu tố chi phối chính hành vi dài hạn của PM2.5, chứ không phải một xu hướng trôi dạt.

Cuối cùng, thành phần Residual dao động khá đều quanh mốc 0, phần lớn nằm trong khoảng $\pm 10$, nhưng thỉnh thoảng xuất hiện những đỉnh nhọn vượt 15--20~$\mu$g/m$^3$ (rõ nhất ở cuối tháng 2/2023 và đầu tháng 4/2025) trùng với các đợt tăng đột biến đã thấy ở biểu đồ giá trị thực tế. Đây là phần biến động không được giải thích bởi Trend hay Seasonal, phản ánh các sự kiện ô nhiễm mang tính tức thời (điều kiện thời tiết bất lợi, hoạt động đốt theo mùa vụ...) — đúng là loại dao động ngắn hạn, khó đoán trước bằng thống kê cổ điển mà thành phần GRU trong kiến trúc hybrid ở Mục~2.3 được thiết kế để đảm nhiệm.

\begin{figure}[H]
\centering
\includegraphics[width=0.65\textwidth]{figures/pm25_seasonal_subseries.png}
\caption{Seasonal Subseries Plot: PM2.5 theo từng tháng (gộp tất cả các năm)}
\label{fig:seasonal}
\end{figure}

Hình~\ref{fig:seasonal} cho thấy PM2.5 đạt đỉnh vào các tháng 1--4 (cao nhất ở tháng 4, trung bình $\approx$20~$\mu$g/m$^3$), giảm dần xuống mức thấp nhất vào các tháng 7--10 (trung bình $\approx$10--12~$\mu$g/m$^3$), rồi tăng nhẹ trở lại vào tháng 11--12. Kiểu biến động này phù hợp với đặc trưng khí hậu khu vực Nam Trung Bộ: các tháng đầu năm (gió mùa Đông Bắc, ít mưa) khiến bụi mịn tích tụ nhiều hơn, trong khi mùa mưa (tháng 7--10) giúp rửa trôi các hạt lơ lửng, làm giảm nồng độ PM2.5.

\subsection*{Ngưỡng an toàn theo chuẩn WHO và Quy chuẩn Việt Nam (QCVN)}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/pm25_who_qcvn.png}
\caption{PM2.5 trung bình ngày so với ngưỡng khuyến nghị WHO và Quy chuẩn Việt Nam (QCVN)}
\label{fig:whoqcvn}
\end{figure}

Trong tổng số 1.453 ngày có dữ liệu, có \textbf{510 ngày (35.1\%)} có nồng độ PM2.5 trung bình 24h vượt ngưỡng khuyến nghị của WHO Air Quality Guideline 2021 (15~$\mu$g/m$^3$) — cho thấy hơn một phần ba số ngày trong giai đoạn quan sát không đạt chuẩn an toàn theo khuyến nghị quốc tế. Ngược lại, \textbf{không có ngày nào (0.0\%)} vượt ngưỡng \textbf{QCVN 05:2013/BTNMT} — \textbf{Quy chuẩn kỹ thuật quốc gia về chất lượng không khí xung quanh}, do \textbf{Bộ Tài nguyên và Môi trường (BTNMT)} ban hành năm 2013 — của Việt Nam (50~$\mu$g/m$^3$) (Hình~\ref{fig:whoqcvn}) — phản ánh khoảng cách đáng kể giữa quy chuẩn hiện hành trong nước và chuẩn khuyến nghị siết chặt hơn của WHO.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/AQI_PM25.png}
\caption{Thang phân loại chỉ số chất lượng không khí (AQI) áp dụng riêng cho PM2.5 (nguồn: AQICN, \url{https://aqicn.org/scale/})}
\label{fig:aqiscale}
\end{figure}

Khác với WHO và QCVN — mỗi chuẩn chỉ đưa ra một ngưỡng nhị phân "đạt/không đạt" — thang Air Quality Index (AQI) ở Hình~\ref{fig:aqiscale} phân loại mức độ ô nhiễm PM2.5 thành \textbf{6 cấp độ} (Good, Moderate, Unhealthy for Sensitive Groups, Unhealthy, Very Unhealthy, Hazardous), mỗi cấp đi kèm khuyến cáo sức khỏe và khuyến nghị hành vi cụ thể, cho phép đánh giá mức độ nghiêm trọng chi tiết hơn nhiều so với việc chỉ so sánh với một ngưỡng duy nhất. Đối chiếu với dữ liệu quan sát được: phần lớn các ngày (trung vị 12.5, trung bình 13.73~$\mu$g/m$^3$ — Bảng~\ref{tab:describe}) chỉ tương ứng mức \textbf{Moderate}, tức chất lượng không khí chấp nhận được và hầu như không gây lo ngại, ngoại trừ nhóm rất nhạy cảm. Tuy nhiên, các đợt tăng đột biến quan sát được ở Hình~\ref{fig:whoqcvn} (đỉnh cao nhất của chuỗi trung bình ngày là 41.3~$\mu$g/m$^3$, ghi nhận ngày 2025-04-05) đã vượt xa khoảng Moderate và bước vào vùng \textbf{Unhealthy for Sensitive Groups} theo các thang phân loại AQI phổ biến (US EPA) mà AQICN sử dụng. Điều này cho thấy mặc dù QCVN Việt Nam (50~$\mu$g/m$^3$) chưa từng bị vượt trong suốt giai đoạn quan sát, kết luận đó chỉ phản ánh một ngưỡng khá rộng — các đợt ô nhiễm đỉnh điểm trong dữ liệu vẫn đủ nghiêm trọng để ảnh hưởng đến nhóm nhạy cảm nếu đối chiếu theo thang đánh giá quốc tế có phân cấp chi tiết hơn.

% ==================== 2.3 TIỀN XỬ LÝ ====================
\section{Tiền xử lý dữ liệu}

Nồng độ PM2.5 theo thời gian có thể trải qua hiện tượng lệch phân phối (distribution shift) — nền ô nhiễm ở một giai đoạn có thể khác biệt đáng kể so với giai đoạn huấn luyện trước đó — mà một mạng nơ-ron huấn luyện một lần rồi giữ cố định trọng số khó thích nghi kịp. Giải pháp được xây dựng theo kiến trúc \textbf{hybrid}, kết hợp hai thành phần bổ trợ cho nhau:

\begin{itemize}
    \item Một thành phần thống kê cổ điển — \textbf{EWMA} (Exponentially Weighted Moving Average) — ước lượng xu hướng/nền hiện tại của chuỗi PM2.5 bằng trung bình có trọng số giảm dần theo cấp số nhân đối với các quan sát càng cũ càng ít quan trọng. Vì được tính trực tiếp từ dữ liệu chứ không qua huấn luyện bằng gradient, thành phần này có thể cập nhật gần như tức thời mỗi khi có thêm quan sát thật mới, bám theo sự thay đổi của nền nhanh hơn nhiều so với việc huấn luyện lại mạng nơ-ron.
    \item Mô hình \textbf{Direct GRU} (đã trình bày ở Mục~1.4) — không còn học trực tiếp PM2.5 tuyệt đối, mà học phần dao động ngắn hạn còn lại sau khi đã trừ đi thành phần xu hướng nói trên.
\end{itemize}

Toàn bộ các bước tiền xử lý và xây dựng mô hình trình bày dưới đây được thiết kế phục vụ trực tiếp cho kiến trúc hybrid này.

\begin{figure}[H]
\centering
\begin{tikzpicture}[>=Latex, x=1cm, y=1cm]
  % ---------- Input ----------
  \node[realcell, minimum width=1.9cm, minimum height=0.9cm] (input) at (0,0.8) {PM2.5};
  \node[font=\scriptsize, align=center] at (0,-0.55) {Chuỗi PM2.5\\theo ngày (thật)};

  % ---------- Nhanh tren: EWMA -> Trend ----------
  \node[gatebox, minimum width=1.7cm, minimum height=0.9cm] (ewma) at (3.6,2.4) {EWMA};
  \draw[cline] (input.north) |- (ewma.west);

  \node[predcell, minimum width=2.1cm, minimum height=0.9cm, fill=yellow!35] (trend) at (7.2,2.4) {Trend $T$};
  \draw[cline] (ewma.east) -- (trend.west);

  % ---------- Nhanh duoi: GRU -> Residual ----------
  \node[grubox, minimum width=1.7cm, minimum height=0.9cm] (gru) at (3.6,-0.8) {GRU};
  \node[font=\scriptsize, align=center] at (3.6,-1.55) {Direct multi-output};
  \draw[cline] (input.south) |- (gru.west);

  \node[predcell, minimum width=2.4cm, minimum height=0.9cm] (resid) at (7.2,-0.8) {Residual $\hat{r}$};
  \draw[cline] (gru.east) -- (resid.west);

  % ---------- Cong lai ----------
  \node[opnode, minimum size=0.6cm, font=\normalsize] (plus) at (10.2,0.8) {$+$};
  \draw[cline] (trend.east) -| (plus.north);
  \draw[cline] (resid.east) -| (plus.south);

  \node[predcell, minimum width=2.6cm, minimum height=1cm, fill=orange!45] (output) at (13.4,0.8) {PM2.5 dự báo};
  \draw[cline] (plus.east) -- (output.west);

  \node[font=\scriptsize, align=center] at (13.4,-0.3) {$\hat{y}_{t+1..t+7} = T_{t+1..t+7} + \hat{r}_{t+1..t+7}$};
\end{tikzpicture}
\caption{Kiến trúc hybrid: EWMA ước lượng Trend, GRU học Residual, cộng lại thành PM2.5 dự báo}
\label{fig:hybrid_arch}
\end{figure}

\subsection*{Loại bỏ đặc trưng không liên quan}

Dựa trên kết quả phân tích tương quan ở Mục~2.2 (Correlation Heatmap, $|r| \leq 0.1$ với PM2.5), ba biến \textbf{Dust}, \textbf{UV Index}, \textbf{UV Index Clear Sky} được loại bỏ khỏi tập đặc trưng đầu vào nhằm giảm nhiễu và số chiều dữ liệu.

\subsection*{Gộp dữ liệu theo ngày}

Ở mức dữ liệu theo giờ, khi horizon dự báo được kéo dài (nhiều giờ đến nhiều ngày tới), sai số có xu hướng tăng nhanh và mô hình trở nên nhạy cảm với nhiễu ngắn hạn giữa các giờ liên tiếp. Vì vậy, toàn bộ dữ liệu (cả tập huấn luyện \texttt{cleaned\_data.csv} lẫn tập dữ liệu mới dùng để đánh giá) được \textbf{gộp về mức trung bình ngày}. Phép gộp này làm mượt các dao động ngắn hạn trong ngày, đồng thời quy đổi horizon dự báo từ đơn vị giờ sang đơn vị ngày — giúp bài toán dự báo nhiều bước trở nên khả thi và ổn định hơn.

\subsection*{Tách xu hướng và phần dư (Decomposition)}

Cụ thể, chuỗi PM2.5 theo ngày được \textbf{phân tách thành hai thành phần} trước khi đưa vào mô hình:

\begin{itemize}
    \item \textbf{Xu hướng (Trend)}: ước lượng bằng EWMA nhân quả — chỉ sử dụng các quan sát trong quá khứ, với độ dài chu kỳ (span) $=7$ ngày.
    \item \textbf{Phần dư (Residual)}: phần chênh lệch còn lại giữa giá trị PM2.5 thực tế và Xu hướng — phản ánh các dao động ngắn hạn quanh nền, có tính ổn định cao hơn nhiều so với chuỗi PM2.5 gốc.
\end{itemize}

Mô hình GRU được huấn luyện để học và dự báo trực tiếp \textbf{thành phần Residual} (thay cho PM2.5 tuyệt đối) ở cả đầu vào lẫn đầu ra. Khi tạo dự báo, giá trị PM2.5 cuối cùng được khôi phục bằng cách cộng Residual do mô hình dự báo với giá trị Xu hướng gần nhất đã biết.

\subsection*{Mã hóa chu kỳ thời gian}

Vì đơn vị thời gian cơ sở giờ đây là ngày, đặc trưng lịch duy nhất còn phù hợp là \textbf{tháng} — được mã hóa dạng \textbf{cyclical encoding} bằng cặp hàm sin/cos nhằm phản ánh đúng tính chu kỳ (tháng 12 và tháng 1 về bản chất "gần nhau"):

\begin{equation}
\text{month\_sin} = \sin\!\left(\frac{2\pi \cdot \text{month}}{12}\right), \qquad
\text{month\_cos} = \cos\!\left(\frac{2\pi \cdot \text{month}}{12}\right)
\end{equation}

Đặc trưng này giúp mô hình nắm bắt được tính mùa vụ của PM2.5 đã quan sát ở Mục~2.2 (nồng độ cao vào các tháng đầu năm, thấp vào mùa mưa giữa năm).

\subsection*{Chia tập Train / Validate / Test}

Tập Test là một \textbf{bộ dữ liệu hoàn toàn mới}, thu thập ở giai đoạn sau đó — mô phỏng sát tình huống triển khai thực tế, khi mô hình phải dự báo cho dữ liệu chưa từng "nhìn thấy" trong suốt quá trình phát triển. Phần dữ liệu còn lại (gọi là \textbf{Pool}) được chia tiếp theo trình tự thời gian thành hai phần tách biệt hoàn toàn: \textbf{Train} (phần đầu, dùng để tạo các sequence huấn luyện) và \textbf{Validate} (300 ngày cuối của Pool, không chồng lấn với bất kỳ sequence huấn luyện nào) — dùng cho Grid Search và early-stopping bằng sliding-fold cross-validation (Mục~2.4.2). Việc tách Validate ra khỏi Train ngay từ đầu là điều kiện bắt buộc để validation loss thực sự phản ánh khả năng tổng quát hóa của mô hình, thay vì chỉ đo lại phần dữ liệu mô hình đã được học.

\begin{table}[H]
\centering
\caption{Phân chia Train / Validate / Test (dữ liệu theo ngày)}
\begin{tabular}{lrll}
\toprule
Tập & Số ngày & Từ & Đến \\
\midrule
Train & 1.153 & 2022-08-04 & 2025-09-29 \\
Validate & 300 & 2025-09-30 & 2026-07-26 \\
Test & 28 & 2026-07-27 & 2026-08-23 \\
\bottomrule
\end{tabular}
\end{table}

\subsection*{Chuẩn hóa dữ liệu}

Toàn bộ đặc trưng số (đã bao gồm thành phần Residual thay cho PM2.5 gốc) được chuẩn hóa về khoảng $[0,1]$ bằng \textbf{MinMaxScaler}. Scaler chỉ được \textit{fit} trên Pool, sau đó dùng để \textit{transform} tập Test — nhằm tránh rò rỉ thông tin thống kê (min/max) của Test vào quá trình huấn luyện.

\subsection*{Tạo cặp \texorpdfstring{$(X, y)$}{(X, y)} cho dự báo đa bước}

Từ dữ liệu đã chuẩn hóa, các cặp $(X, y)$ được tạo bằng kỹ thuật sliding window (Mục~1.1) với \texttt{window\_size} $=28$ ngày và horizon $=7$ ngày. Độ dài cửa sổ quan sát này được xác định bằng thực nghiệm so sánh (7, 14 và 28 ngày, giữ nguyên toàn bộ phần còn lại của pipeline) — cửa sổ 28 ngày cho MAE và RMSE tổng hợp thấp nhất trên tập Test. Với mỗi vị trí $i$, $X_i$ là ma trận gồm 28 bước thời gian $\times$ 9 đặc trưng (PM10, PM2.5 đã thay bằng Residual, Carbon Monoxide, Nitrogen Dioxide, Sulphur Dioxide, Ozone, Aerosol Optical Depth, month\_sin, month\_cos), còn $y_i$ là một \textbf{vector 7 giá trị} — Residual của PM2.5 tại 7 ngày tiếp theo, được dự báo đồng thời trong một lượt duy nhất.

\section{Xây dựng mô hình}

\subsection*{Kiến trúc GRU: Direct Multi-output}

Mô hình giữ nguyên khối GRU 2 lớp (dropout $0.2$) đã trình bày ở Mục~1.4, nhưng lớp fully-connected cuối được đổi thành ánh xạ trạng thái ẩn tại bước thời gian cuối cùng thành \textbf{7 giá trị dự báo đồng thời} thay vì 1 giá trị — đúng chiến lược \textbf{Direct GRU} đã trình bày lý thuyết ở Mục~1.4. Lựa chọn này dựa trên cơ sở: bằng cách dự báo trọn horizon trong một lượt forward duy nhất, Direct GRU không dùng giá trị dự báo của chính nó làm đầu vào cho bước kế tiếp, nên tránh được hiện tượng \textbf{dồn sai số} (error compounding) — hạn chế cố hữu của chiến lược Recursive khi horizon tăng lên. Với 9 đặc trưng đầu vào và \texttt{hidden\_size}$=32$ (giá trị được chọn qua Grid Search ở mục kế tiếp), mô hình có tổng cộng \textbf{10.695 tham số có thể huấn luyện}.

\subsection*{Tinh chỉnh siêu tham số}

Để việc chọn siêu tham số và early-stopping có một tín hiệu đánh giá đáng tin cậy nhưng vẫn tách biệt hoàn toàn khỏi dữ liệu huấn luyện, một cơ chế \textbf{sliding-fold cross-validation} được áp dụng riêng trên tập Validate (không dùng Train): tập Validate được quét bằng nhiều fold $(X, y)$ liên tiếp, mỗi fold gồm 28 ngày làm đầu vào và 7 ngày kế tiếp làm mục tiêu, dịch đi đúng 7 ngày (bằng horizon) sau mỗi fold để phần mục tiêu giữa các fold không chồng lấn — thu được \textbf{38 fold}. Validation loss (MAE, thang chuẩn hóa) dùng cho Grid Search và early-stopping được tính trung bình trên toàn bộ 38 fold này, thay vì dựa trên một khối dữ liệu cố định duy nhất — giúp tín hiệu đánh giá ổn định và đáng tin cậy hơn một khối 7 ngày đơn lẻ, đồng thời vẫn phản ánh đúng khả năng tổng quát hóa vì không chồng lấn với Train.

Grid Search được thực hiện trên không gian hai siêu tham số \texttt{hidden\_size} $\in \{16, 32, 64\}$ và \texttt{learning\_rate} $\in \{0.001, 0.0005\}$ (\texttt{window\_size}$=28$ ngày và horizon$=7$ ngày giữ cố định) — mỗi cấu hình huấn luyện tối đa 20 epoch với early stopping (patience$=5$).

\begin{table}[H]
\centering
\caption{Kết quả Grid Search siêu tham số (val\_loss trung bình trên 38 fold Validate, sắp xếp tăng dần)}
\begin{tabular}{rrr}
\toprule
hidden\_size & learning\_rate & val\_loss (MAE, scaled) \\
\midrule
32 & 0.0010 & 0.08452 \\
32 & 0.0005 & 0.08492 \\
64 & 0.0010 & 0.08517 \\
64 & 0.0005 & 0.08532 \\
16 & 0.0010 & 0.08543 \\
16 & 0.0005 & 0.08690 \\
\bottomrule
\end{tabular}
\end{table}

Cấu hình được chọn là cấu hình có validation loss thấp nhất: \texttt{hidden\_size}$=32$, \texttt{learning\_rate}$=0.001$.

\subsection*{Huấn luyện mô hình cuối cùng}

Với cấu hình đã chọn, mô hình được huấn luyện lại với ngân sách epoch lớn hơn (tối đa 40 epoch, patience$=8$) để hội tụ đầy đủ hơn.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/hybrid_learning_curve.png}
\caption{Learning curve — GRU Residual (hidden\_size=32, horizon=7 ngày): toàn bộ quá trình (trái) và phóng to từ epoch 3 (phải), đường chấm xám đánh dấu epoch tốt nhất}
\label{fig:hybrid_learning_curve}
\end{figure}

Vì Validate lúc này là một tập tách biệt thật sự khỏi Train, đường Validation Loss ở Hình~\ref{fig:hybrid_learning_curve} thể hiện đúng một hành vi huấn luyện điển hình. Do hai epoch đầu tiên giảm rất mạnh (từ $\approx 0.41$ xuống $\approx 0.10$), biểu đồ bên trái tuy cho thấy toàn cảnh quá trình huấn luyện nhưng lại nén phần diễn biến sau đó vào một dải rất hẹp; biểu đồ bên phải (phóng to từ epoch 3) bộc lộ rõ diễn biến thật: Validation Loss tiếp tục giảm cùng nhịp với Train Loss thêm vài epoch, đạt tốt nhất $\approx 0.0852$ (thang chuẩn hóa) tại epoch 13, sau đó chững lại và dao động tăng nhẹ trong khi Train Loss vẫn tiếp tục giảm đều — dấu hiệu mô hình bắt đầu overfit lên tập Train. Cơ chế early stopping (patience$=8$) phát hiện đúng xu hướng này và dừng huấn luyện ở epoch 21, khôi phục lại trọng số tốt nhất tại epoch 13.

\section{Đánh giá mô hình: Walk-forward trên dữ liệu mới}

Vì tập Test là dữ liệu thật hoàn toàn mới và mô hình dự báo trực tiếp trọn horizon 7 ngày trong một lượt, chiến lược \textbf{walk-forward theo từng khoảng thời gian} được áp dụng để tận dụng toàn bộ 28 ngày Test một cách công bằng và đáng tin cậy hơn một lượt dự báo duy nhất. Cụ thể, 28 ngày Test được chia thành \textbf{4 khoảng thời gian liên tiếp}, mỗi khoảng 7 ngày. Với khoảng thời gian đầu tiên, mô hình dự báo dựa trên 28 ngày lịch sử gần nhất đã biết (thuộc Pool). Sau khi giá trị thật của một khoảng thời gian được "quan sát", dữ liệu đó được dùng cho hai việc: (i) cập nhật cửa sổ lịch sử làm đầu vào cho khoảng thời gian kế tiếp, và (ii) \textbf{tinh chỉnh nhẹ} (fine-tune) lại trọng số mô hình — 5 epoch, với tốc độ học giảm 10 lần so với lúc huấn luyện ban đầu — nhằm giúp mô hình thích nghi phần nào với biến động mới quan sát được trước khi dự báo khoảng thời gian tiếp theo. Toàn bộ quá trình chỉ sử dụng giá trị thật đã biết làm đầu vào, không dùng lại dự báo của chính mô hình.

\subsection*{Kết quả dự báo theo từng khoảng thời gian}

\begin{table}[H]
\centering
\caption{Hiệu năng mô hình hybrid trên tập Test theo từng khoảng thời gian walk-forward}
\begin{tabular}{clrr}
\toprule
Khoảng & Thời gian & MAE ($\mu$g/m$^3$) & RMSE ($\mu$g/m$^3$) \\
\midrule
1 & 2026-07-27 -- 2026-08-02 & 2.735 & 3.624 \\
2 & 2026-08-03 -- 2026-08-09 & 4.273 & 5.106 \\
3 & 2026-08-10 -- 2026-08-16 & 1.817 & 2.184 \\
4 & 2026-08-17 -- 2026-08-23 & 2.312 & 2.556 \\
\midrule
\textbf{Tổng hợp (28 ngày)} & & \textbf{2.784} & \textbf{3.553} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/hybrid_prediction_walkforward.png}
\caption{PM2.5: Thực tế và Dự báo walk-forward trên tập Test (đường chấm xám đánh dấu ranh giới giữa các khoảng thời gian)}
\label{fig:hybrid_pred}
\end{figure}

Khoảng thời gian 2 (03/08 -- 09/08) có sai số cao nhất trong 4 khoảng, trùng với giai đoạn PM2.5 thực tế sụt giảm mạnh nhất so với nền lịch sử mà mô hình đã học. Điều này cho thấy mô hình vẫn cần một khoảng thời gian nhất định để thích nghi hoàn toàn với một biến động giảm đột ngột và kéo dài. Ở các khoảng thời gian sau (3 và 4), khi nền PM2.5 mới đã dần ổn định, sai số giảm trở lại xuống mức thấp hơn cả khoảng thời gian đầu tiên.

\subsection*{Phân tích Residual}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/hybrid_residual.png}
\caption{Residual (Thực tế $-$ Dự báo) trên tập Test, tổng hợp cả 4 khoảng thời gian walk-forward}
\label{fig:hybrid_resid}
\end{figure}

Residual tổng hợp trên toàn bộ 28 ngày Test có \textbf{mean $= -2.364\,\mu$g/m$^3$} và \textbf{std $= 2.653\,\mu$g/m$^3$} (Hình~\ref{fig:hybrid_resid}). Giá trị mean âm cho thấy mô hình vẫn còn thiên lệch (bias) hệ thống theo hướng dự báo cao hơn thực tế — hệ quả trực tiếp của việc nền PM2.5 trong giai đoạn Test thấp hơn đáng kể so với những gì mô hình quan sát được trong phần lớn dữ liệu huấn luyện. Độ lệch này chưa được loại bỏ hoàn toàn — cho thấy đây là một hướng còn có thể cải thiện thêm.

\section{Kết luận}

Trong tiểu luận này, bài toán dự báo nồng độ PM2.5 theo chuỗi thời gian tại thành phố Quy Nhơn đã được xây dựng và đánh giá thông qua mô hình mạng GRU. Quy trình thực hiện đầy đủ các bước: làm sạch dữ liệu, phân tích khám phá chuyên biệt cho chuỗi thời gian (xu hướng, tính chu kỳ qua ACF/PACF/Decomposition), tiền xử lý và feature engineering, tinh chỉnh siêu tham số bằng Grid Search, và đánh giá mô hình trên dữ liệu thật hoàn toàn mới.

Điểm nổi bật của giải pháp cuối cùng là kiến trúc \textbf{hybrid}, kết hợp một thành phần thống kê cổ điển (Xu hướng EWMA, thích nghi tức thời với dữ liệu mới) với một mô hình GRU học phần dao động còn lại (Residual) — nhằm đối phó với hiện tượng lệch phân phối (distribution shift) theo thời gian trong dữ liệu PM2.5. Kết hợp với chiến lược đánh giá walk-forward (cập nhật dữ liệu thật và tinh chỉnh nhẹ mô hình theo từng khoảng thời gian), giải pháp đạt \textbf{MAE $=2.784\,\mu$g/m$^3$} và \textbf{RMSE $=3.553\,\mu$g/m$^3$} trên trọn 28 ngày dữ liệu mới.

Bên cạnh những kết quả tích cực, tiểu luận còn một số hạn chế: (1) dữ liệu chỉ trải dài khoảng 4 năm, giới hạn khả năng học đầy đủ chu kỳ năm mà EDA đã chỉ ra là chu kỳ chiếm ưu thế; (2) residual trên tập Test vẫn còn thiên lệch hệ thống (mean $\approx -2.36\,\mu$g/m$^3$), cho thấy thành phần Xu hướng EWMA — dù thích nghi nhanh hơn nhiều so với việc huấn luyện lại mạng nơ-ron — vẫn chưa bám kịp hoàn toàn những biến động giảm đột ngột và kéo dài; (3) do tập dữ liệu mới chỉ có 28 ngày, việc đánh giá theo từng khoảng thời gian walk-forward tuy đáng tin cậy hơn một lượt dự báo duy nhất nhưng vẫn dựa trên số lượng mẫu còn khiêm tốn.

Hướng phát triển tiếp theo có thể tập trung vào: cải tiến thành phần Xu hướng (ví dụ các phương pháp trung bình trượt/làm mượt thích nghi khác, hoặc kết hợp nhiều thành phần theo chu kỳ khác nhau); mở rộng thời gian thu thập dữ liệu mới để việc đánh giá walk-forward có ý nghĩa thống kê vững chắc hơn; và thử nghiệm kiến trúc Seq2seq (Encoder–Decoder) đầy đủ — kiến trúc mà GRU vốn được thiết kế nguyên bản để giải quyết — cho phần Residual, thay cho Direct GRU hiện tại.

\chapter*{Tài liệu tham khảo}
\addcontentsline{toc}{chapter}{Tài liệu tham khảo}

\begin{enumerate}
    \item Zhang, A., Lipton, Z. C., Li, M., \& Smola, A. J. \textit{Dive into Deep Learning}. Cambridge University Press, 2023. \url{https://d2l.ai} — nguồn Hình~\ref{fig:lstm} (Kiến trúc LSTM cell) và Hình~\ref{fig:gru} (Kiến trúc GRU).
\end{enumerate}

\textit{(Danh sách tài liệu tham khảo sẽ được bổ sung thêm.)}

\end{document}
